table:
  name: blocks
  schema: public
select_permissions:
  - role: DIET
    permission:
      columns:
        - name
        - district_id
        - id
        - created_at
        - updated_at
      filter: {}
  - role: OpenRole
    permission:
      columns:
        - name
        - district_id
        - id
        - created_at
        - updated_at
      filter: {}
