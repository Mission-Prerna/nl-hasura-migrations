table:
  name: school_visit
  schema: public
object_relationships:
  - name: mentor
    using:
      foreign_key_constraint_on: mentor_id
  - name: school_list
    using:
      manual_configuration:
        column_mapping:
          school_udise: udise_code
        insertion_order: null
        remote_table:
          name: school_list
          schema: public
select_permissions:
  - role: DIET
    permission:
      columns:
        - school_visit_status
        - time_stamp
        - id
        - school_udise
        - mentor_id
      filter: {}
  - role: manav_sampada
    permission:
      columns:
        - school_visit_status
        - id
        - mentor_id
        - school_udise
        - time_stamp
      filter: {}
