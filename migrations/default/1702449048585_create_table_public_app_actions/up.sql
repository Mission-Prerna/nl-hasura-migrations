CREATE TABLE "public"."app_actions" ("id" serial NOT NULL, "android_actions_id" integer NOT NULL, "mentor_id" integer NOT NULL, "actor_id" integer NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("android_actions_id") REFERENCES "public"."android_actions"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
